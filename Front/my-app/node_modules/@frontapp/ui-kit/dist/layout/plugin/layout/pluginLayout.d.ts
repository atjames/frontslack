import React, { FC } from 'react';
interface PluginLayoutProps {
    /** Content to render for the plugin. */
    children: React.ReactNode;
}
export declare const PluginLayout: FC<PluginLayoutProps>;
export {};
