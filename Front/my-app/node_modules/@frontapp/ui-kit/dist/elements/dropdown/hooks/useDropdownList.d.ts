import React from 'react';
export declare function useDropdownList(children: React.ReactNode): {
    itemsCount: number;
    itemsHeight: number;
    getItemHeight: (index: number) => number;
    renderItem: (index: number) => JSX.Element | null;
};
