import React, { FC } from 'react';
export interface DropdownHeadingProps {
    /** Content to render for the heading. */
    children: React.ReactNode;
}
export declare const DropdownHeading: FC<DropdownHeadingProps>;
